[pytest]
testpaths = tests
markers =
    hardware: requires a connected Gatekeeper
    loopback: requires DAC channel i connected to ADC channel i
    post_flash_health_checks: read-only checks safe to run after every upload
