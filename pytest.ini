[pytest]
testpaths = tests
markers =
    hardware: requires a connected Gatekeeper
    loopback: requires DAC channel i connected to ADC channel i
    upload_smoke: read-only checks safe to run after every upload

